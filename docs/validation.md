# 发布验证记录

验证日期：2026-09-05。

## 格式与资源

- Agent Skills 官方 `skills-ref` 验证通过，固定源提交为 `69ef37e9424c0a7ea9dd2293b559e43ec8176379`。
- 本机内置 skill-creator 的快速校验通过。
- 入口共 54 行，详细写作、排版和导出说明按需读取。
- 界面元数据、默认调用提示及包内资源引用检查通过。

## 工具测试

本地运行 13 项测试，全部通过。包括输入路径、文件保护、资源范围限制、符号链接边界，以及四项真实浏览器测试：

| 场景 | 观察结果 |
|---|---|
| 两页样例 | 导出两页 A4 PDF，中文与数字可提取，逐页预览生成成功 |
| 内容超出页面 | 检查失败，保留诊断预览，不导出 PDF |
| 外部图片 | 网络请求被阻止，检查失败，不导出 PDF |
| 表格出现竖线 | 检查失败，不导出 PDF |

PDF 导出还核对了本地文件链接处理，未保留制作机器的文件路径跳转。已有输出目录会被拒绝，原文件保持不变。

## 视觉检查

样例使用 Playwright 1.62.0 与本机 Chromium 内核 152 导出。实际字体读数为 PingFangSC-Regular 和 PingFangSC-Semibold。

已将最终 PDF 通过 Poppler 转为页面图片，逐页检查两页的中文、三线表、流程图、编号和分页。预览位于本目录。未发现截断、重叠或图形方向错误。

## 检查范围

以上证明当前技能包的格式、资源和渲染工具通过所列检查。人工跨任务评估场景另列在 `evals/scenarios.json`，未将其记为所有客户端的自动触发测试结果。后续报告仍需核对真实资料并完成最终文件验收。
